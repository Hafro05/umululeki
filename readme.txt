-open the folder "lao" in a terminal and run the following command:

"$ pip install -r requirements.txt"

-then run the following command:

"$ python3 manage.py runserver"

-copy the url that appears and paste it in your navigator

-click on "commncer" to start answering the form, and at the end you have your answer